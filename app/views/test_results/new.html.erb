<div id="header">
  <div id="cover">
    <%= image_tag "book_cover_small.jpg" %>
  </div>
  <div class="text">
    <h1>Ne kadar Arızasınız?</h1>
    <h4>Arıza olmak; az seçilen yoldan gitmektir. Ezberi bozuk yaşamaktır. Normallerin 'off-side' çizgisini kayıtsızca ihlal etmektir. İşte size 'arıza' testi! Bakalım ne kadar arızasınız?</h4>
  </div>
  <%= clear %>
</div>


<div id="content">

  <%= error_message_on :test_result, :answers %>

  <% form_for @test_result do |f| -%>
  <ol class="questions">
    <div class="question">
      <% @questions.each do |question| -%>
      <li>
        <%= question.text %>
        <ol class="answers">
          <% question.answers.sort_by { rand }.each do |answer| -%>
          <li>
            <%= radio_button_tag "answers[#{question.id}]", answer.id, @test_result.answer_ids.include?(answer.id) %>
            <%= label_tag "answers_#{question.id}_#{answer.id}", answer.text %>
          </li>
          <% end -%>

        </ol>
      </li>
      <% end -%>
    </div>
  </ol>
  <%= f.submit "Sonucumu göster" %>
  <% end -%>
</div>
